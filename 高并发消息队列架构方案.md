# 高并发消息队列架构方案

> 版本：v0.2（评审修订版）。本文是**架构评审初稿**，P0 项落实后方可作为实施方案；
> 文中容量/分区/延迟数字均为**设计假设**，须经压测验证后固化为基线。

## 1. 设计目标

* 峰值吞吐 **百万级 TPS**（需明确口径：集群总 TPS vs 单 Topic，见 §6）
* 生产确认延迟 **P99 ≤ 10ms**（预算分解见 §5.3），端到端 **P99 ≤ 30ms**
* 可用性 **≥ 99.99%**；RPO/RTO 按业务分级（见 §4.4 故障模型）
* 支持水平扩展、分区内顺序、事务消息、幂等消费、死信治理、Schema 治理

## 2. 总体架构（五层）

```
1. 客户端接入层  批量聚合 · 压缩(zstd/snappy) · Hash(Key)%N 分区路由 · 幂等 Producer
2. Broker 核心层  Reactor 线程模型 · 消费者组协调 · 动态背压 · 配额(quota)
3. 存储复制层     Append-Only Log · mmap/sendfile 零拷贝(视为优化而非容量前提) · 复制确认矩阵
4. 元数据控制面   按所选 MQ 的原生机制(KRaft/Raft/DLedger) · Leader 选举 · ACL/审计
5. 治理可观测层   五类监控指标 · 链路追踪 · DLQ 治理 · 冷热分层 · Schema Registry
```

## 3. 复制与确认策略矩阵（P0，替代笼统"3 副本"）

"副本数"本身不构成可靠性指标，必须成套定义：

| 维度 | 必须回答 | 示例（不可丢消息） |
|------|---------|-------------------|
| 副本数 | 3，且跨 3 个 AZ | `replication.factor=3` |
| 成功确认条件 | 几个同步副本落盘才返回 ACK | `min.insync.replicas=2` + `acks=all` |
| ISR 不足时 | 拒绝写入而非继续接受 | 直接拒绝写，触发告警 |
| Leader 选举 | 什么条件下允许/禁止 | 仅同步副本可当选 |
| 网络分区 | 选可用性还是一致性 | 核心消息选一致性 |
| RPO | 与该策略对应的 RPO | P0=0，P1<100ms，P2 可重放 |

> **注意**：`min.insync.replicas` 不是独立可靠性参数，必须与 `acks=all` 成套出现；
> 不同 MQ 复制语义不同（Kafka=ISR，Redpanda=Raft，Pulsar=BookKeeper quorum），参数不能跨产品套用。

## 4. 技术选型

### 4.1 决策树

```
               吞吐 > 50万TPS / 大数据流批              业务交易 / 复杂路由 / 金融级
                        │                                        │
        ┌───────────────┴───────────────┐            ┌───────────┴───────────┐
   [极低延迟/极简运维]            [存算分离/弹性扩展]   [金融事务/定时/死信]   [复杂订阅/低吞吐]
        │                           │                  │                   │
     Redpanda                    Pulsar             RocketMQ           RabbitMQ
        │
   或 Kafka (KRaft)
```

### 4.2 平台能力模型（P2，防"每业务各用一套 MQ"）

选型前先按统一维度打分：**持久性 / 顺序 / 事务 / 重放 / 保留 / 多租户 / 跨地域复制 / 运维模型**。
决策树保留灵活性，但必须产出一张"各候选 × 能力项"的打分表，避免落地后失控。

### 4.3 关键决策

* **投递语义**：默认 at-least-once + 幂等消费（幂等是**数据一致性设计**，非配置项：需定义 idempotency key 来源、去重表/唯一约束、业务写入与 offset 提交的事务边界）。金融场景才评估端到端 exactly-once，且必须分解为 broker 内部 / producer 事务 / consume-process-produce / 外部副作用幂等 四层分别设计
* **顺序保证**：分区内局部顺序。需明确 Key 路由、producer retry 对顺序的影响、消费重试与 rebalance 期间顺序保持、失败消息的 DLQ 后顺序恢复
* **事务消息**：RocketMQ 原生支持（half message + 本地事务 + 回查），需完整状态机；仅"DB 写后发消息"场景可评估 **Outbox 模式**是否更简单

## 5. SLO 量化与容量规划

### 5.1 吞吐→带宽（容量为**假设**，压测反推）

100万 TPS × 1KB × 3 副本 = 3GB/s ≈ 24Gbps 仅是**写入数据量**，实际还包括：
producer ingress、replication、consumer egress（多 consumer group 会放大）、磁盘读写、压缩解压、TLS、协议开销。

**结论**：3GB/s 不能直接推出"10-12 台 Broker"。需按完整流量模型压测反推节点数；文中数字仅作初始预估。

### 5.2 分区规划（P1，按三维而非单维）

分区数不能只看 TPS，需综合：**MB/s（单分区吞吐）+ consumer 并发 + 故障恢复时间**。
100 万 TPS 预估 100-150 分区仅为起点，压测确认，并配套 **hot partition 检测与自动扩容**。

### 5.3 延迟预算（P1，P99≤10ms 必须可分解可证明）

生产确认延迟预算示例分解：

| 段 | 预算 |
|----|------|
| client batching (linger.ms=2) | 2ms |
| client queue + 网络 RTT | ~2ms |
| broker 排队 + append + 复制 | ~4ms |
| fsync/page cache + 响应 | ~2ms |

> `linger.ms=2` 与 `batch.size=32KB` 是**压测初始值**，非通用最优；须用吞吐/P99 曲线确定。

### 5.4 故障模型与 RPO/RTO（P0）

| 等级 | RPO | RTO | 灾备要求 |
|------|-----|-----|---------|
| P0（清结算/库存） | 0 | ≤5s | 跨 AZ quorum commit，Leader 秒级切换 |
| P1（业务交易） | <100ms | ≤分钟级 | 跨 AZ 同步复制，可容忍少量重放 |
| P2（日志/监控） | 可重放 | 分钟级 | 异步复制，单 Broker 掉电容忍缓存丢失 |

**Failure Matrix**：单 Broker / 多 Broker / 单 AZ / 跨 AZ 分区 / 整 Region / 控制面 / 凭证系统 故障，逐项明确：能否继续读写、RPO、RTO、自动还是人工切换。

## 6. 安全与多租户（P0，原方案短板）

* **认证**：TLS/mTLS，SASL/OAuth，workload identity，服务账号生命周期
* **授权**：topic / consumer group 级权限，生产消费分离，管理 API 权限，运维 RBAC
* **传输**：client↔broker、broker↔broker、跨地域链路加密
* **数据**：磁盘加密、备份加密、密钥轮换；敏感消息进 DLQ 需管控
* **多租户隔离**：producer/consumer/connection quota、topic 配额、租户存储与带宽配额、noisy-neighbor 隔离
* **协议与消息限制**：max message/batch size、max compression ratio、producer rate limit、连接数限制、topic/consumer group 创建权限、DLQ retention 上限
* **审计**：管理面 Audit Log（谁建 topic / 改 ACL / 重置消费位点 / 读敏感 topic），审计日志不得依赖被审计 MQ 存储

## 7. 可观测性（P1，五类指标）

| 类 | 指标 |
|----|------|
| 吞吐 | ingress/egress bytes、messages/s、batch size、compression ratio |
| 延迟 | producer P50/P95/P99、broker append、replication lag、端到端 P99 |
| 复制 | ISR 变化、leader election、replica recovery、controller latency |
| 资源 | CPU、GC、disk IOPS/latency、page cache、NIC、连接数 |
| 消费 | Consumer Lag（**消息数 + 最旧消息时间双维度**）、rebalance 次数、retry rate、DLQ rate |

磁盘水位改用动态策略：**retention + 增长率 + 预计剩余时间 + 归档能力** 预测"还能撑多久"，优于固定 85%。

## 8. 治理

* **背压闭环（P1）**：Consumer 慢→Broker 堆积→Producer 节流，明确 producer quota、queue depth、consumer fetch、retry backoff、circuit breaker、admission control，**防重试风暴**
* **DLQ**：记录原始 topic/partition/offset/message ID、异常、首次/末次失败时间、schema version；支持 **DLQ replay 与原顺序恢复**，而非"垃圾桶"
* **Schema Registry（P1）**：版本 + 兼容性策略 + 校验 + 发布流程，防 producer 破坏性 schema 导致消费雪崩

## 9. 落地路线（压测先行，P1 修正原顺序）

1. 按 §4 选型并出能力打分表 → 建集群（原生元数据机制，去 ZK 依赖）
2. **压测前置**：单 Broker 基准 → 多 Broker 线性扩展 → 复制故障 → rebalance → AZ 故障 → 压力+故障组合 → 长稳（24-72h）
3. 客户端接入：批量 + 压缩 + 分区路由 + 幂等 Producer + 背压闭环
4. 消费侧：消费者数 ≤ 分区数，幂等 + 重试 + DLQ + Schema
5. 监控告警 + Audit Log + Failure Matrix 落地
6. 上线后能力：rolling upgrade、online partition migration、容量扩缩容、数据迁移、版本兼容与 rollback

## 10. 待办（P0 项落实清单）

- [ ] 复制确认策略矩阵按所选 MQ 填具体参数（Kafka=ISR / Redpanda=Raft / Pulsar=BookKeeper quorum）
- [ ] 完整流量模型压测，反推 Broker 数与分区数
- [ ] P99 延迟预算逐段验证
- [ ] 安全模型（TLS/认证/RBAC/租户配额/审计）落地
- [ ] Failure Matrix 与 RPO/RTO 分级评审确认
